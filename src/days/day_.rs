extern crate advent;
use self::advent::*;

use std::collections::HashMap;

pub fn run() {
    let filename = "inputs/day__.txt";
    let inputs = read_inputs(&filename);
}

#[cfg(test)]
mod tests {
    use super::*;
}
